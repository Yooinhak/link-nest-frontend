/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [
      // 소셜 로그인 프로필 이미지
      'lh3.googleusercontent.com',
      'k.kakaocdn.net',
    ],
  },
};

module.exports = nextConfig;
